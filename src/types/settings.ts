export interface Warehouse {
  id: string;
  code: string;
  name: string;
  active: boolean;
  createdAt: string;
}

export interface VehiclePosition {
  id: string;
  code: string;
  name: string;
  active: boolean;
  createdAt: string;
}

export interface TransportCode {
  id: string;
  code: string;
  name: string;
  active: boolean;
  createdAt: string;
}
